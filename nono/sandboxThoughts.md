# Experimenting with Commands
*September 23*

I have mostly been running pi with nono throughout the class and was somewhat familiar with the fact that it can only access the files within the working directory. I tried playing around with getting it to run commands such as `git add .` and `git commit`, not expecting it to work. It is pretty good at finding workarounds and was able to add by using the temp folder. This is something to keep in mind for the future to consider with commands that I wouldn't expect it to be able to run.
